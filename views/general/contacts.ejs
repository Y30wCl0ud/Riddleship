<%- include('../partials/header.ejs') %>
<nav>
  <ul>
    <li>
      <a href="mychats">chat</a>
    </li>
    <li class="location">
      <a href="contacts"><b>contacts</b></a>
    </li>
  </ul>
</nav>
<%# loop through all contacts of the user %>

  <main class="contentList">
    <ul>
      <% for (var i = 0; i < results.length; i++) { %>
        <% if (results[i].name === myName) { %>
          <% continue; %>
        <% } %>
        <li>
          <a href="profile/<%= results[i].userID %>">
            <p> <b><%= results[i].name %></b> </p>
          </a>
          <button> menu </button>
        </li>


        <ul class="userMenu hide">
        <% if (admin) { %>
          <li>
            <a href="chat/<%= results[i].userID %>">send message</a>
          </li>
          <li>
            <a href="profile/<%= results[i].userID %>">show profile</a>
          </li>
          <li>
            <a href="profile/<%= results[i].userID %>">delete contact  atm</a>
          </li>
          <% if (results.banned === 1) { %>
            <li>
              <a href="/users/unban/<%= results[i].userID %>">unban user</a>
            </li>
          <% } else { %>
            <li>
              <a href="/users/ban/<%= results[i].userID %>">ban user</a>
            </li>
          <% } %>

        <% } else { %>
          <li>
            <a href="profile/<%= results[i].userID %>">show profile</a>
          </li>
          <li>
            <a href="profile/<%= results[i].userID %>">report user</a>
          </li>
        <% } %>
        </ul>

      <% } %>


    </ul>
  </main>

  <script src='/scripts/main.js'></script>
<%- include('../partials/footer.ejs') %>
