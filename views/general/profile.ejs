<%- include('../partials/header.ejs') %>

  <header>
    <!-- if else needed to place correct link per role -->
    <% if (admin) { %>
      <a href="/users">back</a>
    <% } else { %>
      <a href="/meet">back</a>
    <% } %>

    <% if (admin) { %>
      <a href="menu"> menu </a>
    <% } %>
  </header>

  <section class="profilePic">
    <% if (results.picture) { %>
      <img src="uploads/<%= results.picture %>" alt="">
    <% } else { %>
      <p>No photo added yet.</p>
    <% } %>
  </section>

  <section class="userInfo">
    <h2> <%= results.name %> </h2>
    <p>, <%= results.age %> <%= results.gender %></p>

    <% if (results.location) { %>
      <p> <%= results.location %> </p>
    <% } %>

    <p>Looking for a: <%= results.lookingFor %></p>

    <h3>About me</h5>
    <p>
      <%= results.about %>
    </p>
  </section>


  <div class="menu">
    <% if (admin) { %>
      <p>
        menu
      </p>
      <ul>
        <li><a href="/chat/<%= results.userID %>">send message</a></li>
        <li><a href="block">ban user</a></li>
      </ul>
    <% } else { %>
      <!-- <a href="riddle"><button>send riddle request</button></a> -->
      <a href="/chat/<%= results.userID %>"><button>send riddle request</button></a>
    <% } %>
  </div>




<%- include('../partials/footer.ejs') %>
